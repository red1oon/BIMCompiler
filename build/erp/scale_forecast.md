# Production Scale Forecast — Fold Engine

> Generated by `scripts/poc_scale_forecast.js`. **Measured** cells are timed on the real fold engine;
> **(forecast)** cells are extrapolations of a least-squares fit to those measured points. Envelope
> inputs are `(assumption)`; engine constants (ops/doc, bytes/op, throughput, fit slopes) are measured.

## Measured engine constants

| Constant | Value | Source surface |
|---|---|---|
| ops/doc (full lifecycle) | **3** rows | §SCALE-OPSDOC |
| GL lines per posted doc | 7.1 | §SCALE-MIX (oracle) |
| per-write fit | `0.39 + 8.54e-3·D` ms (R²=0.999) | S-WRITE |
| write crosses 100ms @ D | 11,664 live docs | S-WRITE |
| genesis-boot fit | `1.48 + 1.77e-4·N` ms (R²=1.000) | S-BOOT |
| checkpoint-boot | **~0ms FLAT** (folds 1000-op open period) | S-BOOT |
| projection bloat | 520 B/op | S-STORE (I-3) |
| throughput | naive 7616 → batch **29104 op/s** (3.8× ) | S-THRU |

## Capacity forecast — docs/day & ERP department

**Engine throughput ceiling: ~279,396,418 docs/day** (batch path, flat-out) — orders of
magnitude above any human department, so **aggregate throughput never binds**. Dept size below is derived
from each tier's *actual* annual volume (the department that generates it), not from this ceiling.

Assumptions: 8h working window, 250 working days/yr, clerk band 50–200 posted docs/clerk/day *(tune to your org)*.

| Tier | docs/yr | docs/day | **ERP dept** | live-doc D | write/save (desktop) | write/save (mobile ×4) | total-op N | boot genesis (desktop / mobile) | boot ckpt | DB size | throughput used | **binds** |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| small | 5,000 | 20 | **~1–1 clerks** | 500 | 5ms (measured) | 19ms (est) | 0.01M | 0.0s / 0.0s (measured) | 0ms | 7.4MB | 0.000% | none — headroom (0.000% throughput used) |
| mid | 50,000 | 200 | **~1–4 clerks** | 5,000 | 43ms (measured) | 172ms (est) | 0.45M | 0.1s / 0.3s (forecast) | 0ms | 223.2MB | 0.000% | none — headroom (0.000% throughput used) |
| large | 500,000 | 2,000 | **~10–40 clerks** | 50,000 | 427ms (forecast) | 1710ms (est) | 7.50M | 1.3s / 5.3s (forecast) | 0ms | 3720.7MB | 0.001% | S-WRITE (427ms/save @ 50,000 live docs) |

**Reading it:** throughput utilisation stays a fraction of a percent at every tier — the engine is never
throughput-bound. The only surface that crosses budget is **interactive Save latency** (O(live docs)), which
hits 100ms at ~11,664 live docs desktop / ~2,881 live docs mobile (×4 est) — a working-set limit, managed by scoping/period-close, not a hardware wall.

### Mitigation deltas (what each deployed strategy buys)

| Strategy | OFF | ON | Effect |
|---|---|---|---|
| Checkpoint bootstrap | genesis O(N): 18s @100M ops (mobile cliff) | ~0ms flat | removes the cold-start freeze |
| Batch commitGroup | naive 7616 op/s | 29104 op/s | 3.8× docs/day |
| Projection prune | 520 B/op unbounded | bounded to ops-since-checkpoint | caps DB/RAM growth |

*All forecast numbers trace to the §SCALE-* log lines in `build/erp/poc_scale_forecast.log`.*